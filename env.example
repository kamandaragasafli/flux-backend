# Django Settings
DJANGO_SECRET_KEY=change-me-in-production-generate-a-secure-key
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=206.189.53.174,localhost,127.0.0.1

# Database Configuration
# Local development için SQLite kullan (true)
# Server/Production için PostgreSQL kullan (false)
USE_SQLITE=true

# PostgreSQL ayarları (USE_SQLITE=false olduğunda gerekli)
DB_NAME=flux_tracker
DB_USER=postgres
DB_PASSWORD=your-secure-password
DB_HOST=localhost
DB_PORT=5432

# CORS Settings
# Expo Go için true yapın (farklı IP'lerden bağlanabilir)
CORS_ALLOW_ALL_ORIGINS=true
# If CORS_ALLOW_ALL_ORIGINS is false, add specific origins in settings.py CORS_ALLOWED_ORIGINS

# Security Settings (Production)
SECURE_SSL_REDIRECT=true

# External System Integration - Solvey Pharma
# Yöntem 1: API'den veri çekme (Domain/IP ile)
EXTERNAL_API_URL=https://solveypharma.com.az
EXTERNAL_API_KEY=

# Yöntem 2: İkinci database'den veri çekme (Solvey Database)
USE_EXTERNAL_DB=true
EXTERNAL_DB_NAME=Solvey-2
EXTERNAL_DB_USER=flux_readonly
EXTERNAL_DB_PASSWORD=20012001Kamandar
EXTERNAL_DB_HOST=64.226.72.85
EXTERNAL_DB_PORT=5432

# Solvey Database Tablo İsimleri (Django model isimleri)
# Django genellikle app_name_model_name formatında tablo oluşturur
# Örnek: tracking_doctors, tracking_region, tracking_city, tracking_hospital
SOLVEY_DOCTORS_TABLE=tracking_doctors
SOLVEY_REGIONS_AREA_TABLE=tracking_region
SOLVEY_REGIONS_HOSPITAL_TABLE=tracking_hospital
SOLVEY_CITY_TABLE=tracking_city

# Sıralama kolonları (opsiyonel)
SOLVEY_DOCTORS_ORDER_BY=ad
SOLVEY_REGIONS_AREA_ORDER_BY=region_name
SOLVEY_HOSPITALS_ORDER_BY=hospital_name
SOLVEY_CITY_ORDER_BY=city_name

